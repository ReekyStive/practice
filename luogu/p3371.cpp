#include <iostream>
#include <algorithm>

using namespace std;

struct Edge
{
    int from;
    int to;
    int len;
    Edge(int a, int b, int c)
    {
        from = a;
        to = b;
        len = c;
    };
};

int main()
{
    int n, m, s;
    return 0;
}