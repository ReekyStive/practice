#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
    int n, a, b, g, k;
    int val[201] = {-1};
    cin >> n;

    for (int i = 0; i < n; i++)
    {
        cin >> a >> b >> g >> k;

    }
    return 0;
}