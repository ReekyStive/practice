#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
    char a;
    cin >> a;
    cout << a << endl;
    cout << "A: " << (int)'A' << "    Z: " << (int)'Z' << endl;
    cout << "a: " << (int)'a' << "    z: " << (int)'z' << endl;
    cout << "\": " << (int)'\"' << endl;

    return 0;
}
